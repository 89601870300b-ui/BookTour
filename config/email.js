/*const nodemailer = require('nodemailer');

const transporter = nodemailer.createTransport({
    host: 'smtp.yandex.ru',
    port: 465,
    auth: {
        user: 'apikey', // это слово, если вы используете API-ключ
        pass: process.env.SENDGRID_API_KEY
    }
});*/